# 해당 프로젝트의 목적은 QR 코드를 이미지화 시켜 디코딩 한뒤 브라우저에서 해당 링크를 열어주는걸 목표로 합니다.
# 사용언어 , 개발도구 , 언어는 상관없으며, 다른 프로그램이 열려있어도 그곳에서 QR을 가져올수 있어야합니다.
# 실행 -> 버튼클릭 -> 윈도우의 스크린샷처럼 크로스표시가 나오고 드래그 엔 드랍을하면 해당 부분이 이미지 상태로 클립보드에 저장 -> 그 이미지로부터 큐알코드만 가져온뒤 디코딩 -> 웹 브라우저에서 해당 링크를 열어줌

# 제미나이 추천트리
Python + PyQt6 (또는 PySide6)
스크립트 언어의 간결함과 강력한 GUI 프레임워크를 결합한 방식입니다. 시스템 유틸리티 개발에 매우 적합하며 코딩량이 상대적으로 적습니다.

개발 도구: Cursor, PyCharm

핵심 라이브러리: mss (고속 크로스 플랫폼 화면 캡처), pyzbar (QR/바코드 디코딩), Pillow (이미지 처리)

진행 방식:

오버레이 창 생성: PyQt의 QMainWindow를 투명 배경 및 Frameless 속성으로 생성합니다. paintEvent를 재정의하여 마우스 드래그 시 반투명한 사각형을 화면에 그립니다.

영역 캡처: 마우스 버튼 릴리스 시 좌표를 계산하고, mss 라이브러리를 호출하여 해당 좌표 영역의 화면을 캡처합니다. (운영체제별 캡처 API를 자동으로 처리해 줍니다.)

클립보드 저장: 캡처된 이미지를 QClipboard를 통해 시스템 클립보드에 전송합니다.

QR 디코딩: 이미지 객체를 pyzbar.decode 함수에 입력하여 URL을 추출합니다.

브라우저 실행: Python 내장 모듈인 webbrowser.open(url)을 실행합니다.

---

# 개선된 프로젝트 명세 (추천 방식: Python + PySide6 + mss + OpenCV(+pyzbar fallback))

## 프로젝트 목표
사용자가 단축키 또는 버튼으로 화면의 임의 영역을 선택하면,
해당 영역에서 QR 코드를 인식해 URL을 추출하고 기본 브라우저로 연다.

## 1차 범위 (MVP)
- macOS 우선 지원 (추후 Windows 확장)
- 단일 QR(가장 흔한 케이스) 안정적으로 열기
- QR이 URL이 아니면 결과 텍스트를 복사/표시

## 사용자 플로우
1. 실행
2. 버튼 클릭(또는 단축키) → 캡처 모드 진입
3. 십자 커서 + 드래그로 영역 선택
4. 선택 영역 캡처
5. QR 디코딩 → URL이면 브라우저 오픈 / 아니면 텍스트 처리

## 기능 요구사항
- **오버레이 캡처 모드**: 다른 앱 위에서도 전체 화면을 덮는 투명 오버레이로 영역 선택 가능해야 함
- **캡처**: 드래그 영역을 이미지로 캡처
- **클립보드(옵션)**: 캡처 이미지를 시스템 클립보드에 저장 (기본값 ON, 설정으로 OFF 가능)
- **디코딩**:
  - 1순위: `opencv-python`의 `cv2.QRCodeDetector`
  - 2순위(옵션/대안): `pyzbar` fallback (OpenCV 실패 시)
- **결과 처리**:
  - URL이면 `webbrowser.open(url)`
  - URL이 아니면: 텍스트를 클립보드에 복사 + 토스트/다이얼로그로 표시

## 예외/엣지 케이스 처리
- **QR 없음**: "QR 코드를 찾지 못했습니다." 안내
- **여러 QR**: 가장 큰 바운딩 박스 우선(간단 규칙) 또는 리스트 선택(2차 개선)
- **브라우저 실행 실패**: URL을 클립보드에 복사하고 안내 메시지 표시
- **너무 작은 영역/빈 영역**: 재시도 안내

## 비기능 요구사항
- **성능**: 캡처 → 디코딩 → 액션까지 평균 1.5초 이내 목표
- **안정성**: 실패 사유(권한/디코딩 실패 등) 사용자에게 최소한의 메시지 제공
- **로그(옵션)**: 로컬 로그 파일에 실패 사유 기록(2차)

## 권한/보안 (macOS 필수 포인트)
- **화면 녹화(Screen Recording) 권한**이 필요할 수 있음 → 최초 실행 시 안내
- 자동으로 URL을 여는 동작은 사용자 설정으로 ON/OFF 가능(2차)
  - 1차(MVP)는 자동 오픈을 기본으로 하되, 실패 시 복사/안내로 안전장치 제공

## 기술 스택 (추천)
- Language: **Python 3.11+**
- GUI/오버레이: **PySide6**
- Screen capture: **mss**
- Decode: **opencv-python** (기본) + **pyzbar**(fallback, 선택)
- Image: Pillow(필요 시)
- Packaging: **PyInstaller**

## 개발 진행 체크리스트 (바로 착수용)
- [ ] PySide6로 전체 화면 투명 오버레이(Frameless + always-on-top) 구현
- [ ] 드래그로 선택 사각형 렌더링 + 좌표 계산
- [ ] `mss`로 영역 캡처 → PIL/numpy 변환 파이프라인
- [ ] OpenCV `QRCodeDetector`로 디코딩 구현
- [ ] URL 판별(간단: `urllib.parse` 기반) 후 `webbrowser.open`
- [ ] 실패/예외 메시지(다이얼로그) 최소 구현
- [ ] PyInstaller로 macOS 실행 파일 빌드 스크립트/문서화